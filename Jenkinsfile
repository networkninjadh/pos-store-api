pipeline {

    agent any

    stages {

        stage('Initialize') {
            steps {
                echo "PATH = ${PATH}"
                sh pwd
            }

        }

        stage('Build') {
            steps {
                echo "Build Stage"
            }
        }

        /*
        stage('Test') {
        }

        stage('package') {

        }
        */
    }
}
